# 小鱼干（积分）系统 · 产品文档

> 状态：待评审 ｜ 适用端：服务端（tornado）+ App（RN/Expo）+ 网页前台 + 后台管理
> 目标：用积分（小鱼干）统一管理聊天、插图、建角色等操作的消耗与限制，并为后续付费做铺垫。

---

## 1. 背景与目标

当前消耗型操作各自为政：插图/头像有「每日张数上限」，聊天和建角色无任何限制。我们引入统一的**积分（小鱼干）**货币：

- 用户做消耗型操作扣积分，余额不足则**硬拦截**。
- 通过**每日任务**获取积分，首期上线**每日签到**（含连续签到天数）。
- 所有积分数值（消耗/奖励）**后台可配置**。
- 为将来「充值购买积分」预留结构。

### 核心决策（已确认）
| 决策点 | 结论 |
|---|---|
| 余额不足 | **硬拦截**：不足以支付即拒绝该操作 |
| 聊天扣费时机 | **成功才扣**：LLM 成功返回回复后扣，中途出错不扣 |
| 与现有每日张数上限的关系 | **积分取代上限**：插图/头像不再按张数限制，改为纯积分扣费 |
| 新用户 | **注册赠送初始积分**（数值后台可配） |

---

## 2. 名词

- **小鱼干**：积分的产品名（UI 文案用「小鱼干」，代码/接口用 points）。
- **余额**：用户当前可用小鱼干数。
- **流水**：每次积分增减的记录（含原因、变动值、变动后余额）。
- **连续签到天数（streak）**：连续每天签到的天数；断签归零。

---

## 3. 积分消耗规则

| 操作 | 触发点（现有接口） | 扣费 | 备注 |
|---|---|---|---|
| 普通聊天（一条回复） | `POST /sessions/:id/chat` | `cost_chat`（x） | LLM 成功返回后扣 |
| 带语音聊天（本条回复触发 TTS） | 同上 | `cost_chat_voice`（a） | 判定 TTS 是否启用：用户开关 + 角色有 voice_id；启用则按带语音价扣，**替代**普通聊天价（不叠加） |
| 生成场景插图 | `POST /sessions/:id/scene-image` | `cost_image`（y） | |
| 生成角色卡片图 | `POST /character/cards/generate` | `cost_image`（y） | 与插图同价 |
| 生成情绪头像（单个） | `POST /avatars/:mood/regenerate` | `cost_avatar`（v） | |
| 生成情绪头像（一键全部） | `POST /avatars/regenerate` | `cost_avatar × 情绪数` | 按实际生成张数 × 单价 |
| 创建角色 | `POST /characters` | `cost_create_character`（z） | 仅创建记录时扣，见下方 ⚠️ |

> ⚠️ **创建角色时不收图片费**：创建角色时会首次生成该角色的**角色卡片**和**全部情绪头像**（激活时触发 `pregenerateMoodAvatars`）。这些由创建角色自带、**不额外扣积分**（已包含在 `cost_create_character` 中）。之后用户手动点击「重生成角色卡片」「重生成头像」才按上述单价扣。

### 3.1 聊天带语音的判定
带语音 = 该条回复实际会走 TTS。服务端已有判定：用户设置 TTS 开关开启 **且** 当前角色配有 `voice_id`。满足则按 `cost_chat_voice` 扣，否则按 `cost_chat`。两者二选一，不叠加。

### 3.2 「积分取代每日张数上限」
- 移除（或置为不生效）现有 `daily_scene_image_limit` / `daily_avatar_image_limit` 的张数拦截。
- 插图、角色卡片统一按 `cost_image` 扣积分；情绪头像按 `cost_avatar` 扣；余额不足则拒绝。
- 「一键重生成全部情绪头像」按生成张数 × `cost_avatar` 扣（批量）。

> 注：现有按天计数字段（user_settings 里的 *_date / *_count）可保留用于统计，但不再作为拦截依据。

### 3.3 扣费与回滚原则
- **聊天**：成功返回回复后扣；流式中途出错 / LLM 失败 → 不扣。开扣前先校验余额够，不够直接拒绝（不发起 LLM）。
- **插图 / 角色卡片 / 情绪头像**：进入实际生成流程前校验 + 扣；若生成任务**提交失败**（同步阶段即失败）则不扣或退还。异步生成失败的退款见 §9.2。
- **创建角色**：写入角色记录成功即扣。创建角色时触发的首次卡片/头像生成不扣（见上方 ⚠️）。
- 所有扣费写一条流水。

---

## 4. 积分获取规则（首期）

### 4.1 注册赠送
新用户注册成功时，发放 `signup_bonus` 初始积分（后台可配），写一条流水（reason=`signup`）。

### 4.2 每日签到
- 入口：App【我的】tab 新增「每日签到」卡片。
- 规则：每自然日可签到一次，获得 `checkin_base` 积分。
- 连续签到：连续天数 streak 累加；断签（跨过一天未签）归零重新计。
- 连续奖励（可选，后台开关）：streak 达到阈值额外加成，首期可简化为「基础分 + streak 封顶的小幅递增」或纯基础分，具体见配置项 §6。
- 页面展示：当前余额、今日是否已签、连续签到天数、最近签到记录列表。

> 首期只做签到这一种每日任务；结构上预留「任务类型」扩展（看视频、邀请好友等以后加）。

---

## 5. 数据模型（建议）

沿用现有风格：MySQL、`CREATE TABLE IF NOT EXISTS`、utf8mb4、时间用 `VARCHAR(64)` 存 ISO 字符串、`db.js` 启动自动建表。

### 5.1 用户余额：`user_points`
```
user_id        INT       PRIMARY KEY   -- 一行一用户
balance        INT       NOT NULL DEFAULT 0
updated_at     VARCHAR(64)
```
余额单独存一行，扣加费时事务内更新；并发安全见 §8。

### 5.2 积分流水：`point_transactions`
```
id             INT AUTO_INCREMENT PRIMARY KEY
user_id        INT NOT NULL
delta          INT NOT NULL            -- 正=获取，负=消耗
balance_after  INT NOT NULL            -- 变动后余额（便于审计/展示）
reason         VARCHAR(64) NOT NULL    -- chat / chat_voice / image / create_character / checkin / signup / admin_adjust ...
ref            VARCHAR(128)            -- 关联对象（如 session_id / character_id），可空
created_at     VARCHAR(64) NOT NULL
KEY idx_user_created (user_id, created_at)
```

### 5.3 签到记录：`daily_checkins`
```
id             INT AUTO_INCREMENT PRIMARY KEY
user_id        INT NOT NULL
checkin_date   VARCHAR(16) NOT NULL    -- YYYY-MM-DD
points         INT NOT NULL            -- 当次获得
streak         INT NOT NULL            -- 当次连续天数
created_at     VARCHAR(64) NOT NULL
UNIQUE KEY uq_user_date (user_id, checkin_date)
KEY idx_user_created (user_id, created_at)
```
连续天数也可冗余存到 `user_settings`（last_checkin_date + checkin_streak）便于快速读取；签到历史从本表查。

---

## 6. 后台可配置项（积分管理）

走现有 `global_settings`（键值表）+ `/admin/global-settings`，新增「积分管理」板块。键名建议：

| 键 | 含义 | 默认（占位，待定） |
|---|---|---|
| `points_enabled` | 积分系统总开关（关=不扣不限） | 1 |
| `cost_chat` | 普通聊天每条 | 1 |
| `cost_chat_voice` | 带语音聊天每条 | 3 |
| `cost_image` | 场景插图 / 角色卡片图每张 | 5 |
| `cost_avatar` | 情绪头像每张（单个重生成/批量） | 3 |
| `cost_create_character` | 创建角色（含首次卡片+头像，之后重生成单独按图片计） | 20 |
| `signup_bonus` | 注册赠送 | 100 |
| `checkin_base` | 每日签到基础积分 | 10 |
| `checkin_streak_bonus` | 连续签到每天额外加成（×天数，可封顶） | 2 |
| `checkin_streak_cap` | 连续加成封顶天数 | 7 |

> 数值全部占位，上线前由你定。`points_enabled=0` 时全系统旁路（便于灰度/回滚）。

### 6.1 后台板块
- 在后台导航新增「积分管理」tab（与「系统设置」并列，或并入系统设置的一个分组）。
- 含：上述数值的表单（保存走 PATCH /admin/global-settings）。
- 含：**用户积分查询/调整**——按用户名查余额、手动加减积分（reason=`admin_adjust`，写流水）。
- 含（可选）：签到记录 / 流水查询，便于排查。

---

## 7. 各端展示与交互

### 7.1 App（RN）
- **余额展示**：【我的】tab 顶部账号卡片旁或下方显示「🐟 小鱼干 余额」。聊天页可在头部轻量展示余额（可选）。
- **每日签到卡片**（【我的】tab）：
  - 显示今日是否已签、点击「签到」按钮 → 调签到接口 → toast 显示「+N 小鱼干」。
  - 显示连续签到天数（如「已连续签到 5 天」）。
  - 下方「签到记录」列表（日期 + 当次积分 + streak）。
  - 复用现有列表/卡片样式（参考成就页 FlatList 模式）、`toast`/`confirm`。
- **余额不足拦截**：聊天发送、生成插图、创建角色等接口返回「积分不足」错误码时，弹提示「小鱼干不足，去【我的】签到获取」并引导跳转。

### 7.2 网页前台
- 现状无顶部用户栏。建议在侧栏品牌下方或右侧面板顶部加一个小鱼干余额展示（轻量）。
- 网页端的扣费由服务端统一处理；余额不足时插图按钮等给出与现有 429 一致的 toast 提示文案改为「小鱼干不足」。
- 网页是否做签到入口：首期可不做（签到仅 App），仅展示余额；待定（§8）。

### 7.3 后台
见 §6.1。

---

## 8. 接口设计（建议）

### 用户侧
- `GET /points` → `{ balance }`（当前用户余额）
- `GET /points/transactions?limit=N` → 流水列表
- `GET /checkin/status` → `{ checked_today, streak, today_reward_preview }`
- `POST /checkin` → 执行签到，返回 `{ ok, points, streak, balance }`；已签到返回明确错误
- 各消耗接口（chat/scene-image/cards/characters）内部扣费，余额不足统一返回 **HTTP 402 或 429** + `{ error: "points_insufficient", need, balance }`（错误码二选一，建议 402 Payment Required 语义更准）

### 管理侧
- `GET /admin/points/settings`（或并入 /admin/global-settings）
- `PATCH /admin/global-settings`（新增积分相关键）
- `GET /admin/points/user?username=` → 查某用户余额 + 近期流水
- `POST /admin/points/adjust` → `{ user_id, delta, reason }` 手动加减

---

## 9. 边界与规则细则（已确认）

1. **并发扣费安全**：余额更新用原子判断 `UPDATE user_points SET balance = balance - ? WHERE user_id = ? AND balance >= ?`，影响行数=0 即视为余额不足，避免并发超扣。加积分同理用原子 `+`。流水在同一事务内写。
2. **异步生成失败退款**：插图/角色卡片为异步生成。**提交成功即扣**；若生成最终失败，**系统自动退还**该笔积分（写一条 `reason=refund_image` 的正向流水，`ref` 关联原扣费对象）。退款幂等，避免重复退。
3. **聊天「成功」的界定**：以**回复落库成功**为扣费点（assistant 消息成功写入即扣）。流式中途断开 / LLM 报错且未落库 → 不扣。
4. **重新生成 / 自动模式 / AI 主动消息**（已确认）：
   - **重新生成回复**：**扣费**，等同一条新回复（按当次是否带语音决定 `cost_chat` / `cost_chat_voice`）。
   - **自动模式**：**扣费**，按真实产生的回复条数逐条扣。
   - **AI 主动消息**：**扣费**，每条按 `cost_chat`（带语音则 `cost_chat_voice`）扣。
     - ⚠️ 注意：主动消息由系统在后台触发，扣费前需校验余额；**余额不足时跳过该次主动消息**（不发送、不扣、不报错），避免欠费或打断。
5. **来电（语音通话）**（已确认）：**不计消耗**，不扣积分。
6. **网页端签到**（已确认）：**网页端也支持签到**。签到接口与 App 共用（`GET /checkin/status` + `POST /checkin`），网页前台新增签到入口与连续天数/记录展示。同一用户 App 与网页共享同一份签到状态（按 `user_id` + 自然日唯一）。
7. **时区**：签到「自然日」以服务器时区为准（与现有按天计数一致，用 ISO date 前 10 位 `YYYY-MM-DD`）。
8. **积分系统总开关 `points_enabled=0`**：旁路全部扣费与拦截（含主动消息的余额校验），用于灰度与紧急回滚。

---

## 10. 实施阶段建议（供排期，非本次开发）

- **阶段一（后端基建）**：建表（user_points / point_transactions / daily_checkins）+ 余额原子读写工具 + 流水 + 注册赠送 + global_settings 配置项 + `points_enabled` 总开关。
- **阶段二（扣费接入）**：在 chat / scene-image / cards / characters 四处接入扣费 + 硬拦截；重新生成、自动模式、主动消息按 §9.4 接入；插图/卡片异步失败退款（§9.2）；移除张数上限拦截。
- **阶段三（签到）**：签到接口 + daily_checkins 表 + streak 逻辑（App 与网页共用）。
- **阶段四（App UI）**：【我的】tab 签到卡片 + 余额展示 + 不足拦截提示。
- **阶段五（后台）**：积分管理板块（配置 + 用户余额查询/调整）。
- **阶段六（网页）**：余额展示 + 签到入口 + 不足提示文案。

> 本文档仅为产品设计，不含具体代码实现。数值、待定项确认后再进入开发。
